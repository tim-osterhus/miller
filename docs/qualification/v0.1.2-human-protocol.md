# Miller v0.1.2 owner-visible M1 protocol

Status: `LIVE_NOT_RUN`

Headless terminal result: `HEADLESS_RELEASE_READY_HUMAN_GATE_NOT_RUN`

This protocol is the precise owner-visible release gate. It was not run by the
Packet 6 worker. Headless tests, package verification, and Packet 5 evidence do
not promote any row below to a human pass.

## Required M1 checks

| Check | Result |
| --- | --- |
| External Codex readiness/timeout plus one typed turn | `LIVE_NOT_RUN` |
| Overlay/full-window selection and Command-C | `LIVE_NOT_RUN` |
| GPT-Live speech/transcript/interrupt/end/second session/cleanup | `LIVE_NOT_RUN` |
| Default/custom wake phrase | `LIVE_NOT_RUN` |
| Typed fallback with wake disabled and Live unavailable | `LIVE_NOT_RUN` |
| Reset/removal/relaunch with no lingering helper or microphone owner | `LIVE_NOT_RUN` |

## Boundary facts

- Codex remains a separately installed external prerequisite. Miller does not
  bundle, download, update, or remove it.
- The first wake phrase is **Hey Miller**. One bounded custom English phrase is
  supported; wake listening defaults off and uses the system-default microphone.
- The wake phrase is activation only. It must open Miller and start one ordinary
  Live session without another click. Miller then briefly acknowledges that it
  is ready and listening; speech after the acknowledgement is the request.
- Packet 3 ended `INCONCLUSIVE`. It did not authorize typed input during an
  active Live session, `initialItems`, persistence changes, or attachments.
- Packet 5 is headless-approved with its microphone, custom-phrase, automatic
  Live start, and audible acknowledgement gate still `LIVE_NOT_RUN`.
- Signing, notarization, tagging, pushing, publishing, and owner approval were
  not run by this closure.

Do not retain transcript text, audio, SDP, credentials, provider payloads,
machine-specific locations, or runtime logs while performing this protocol.
Record only the result vocabulary and sanitized failure codes.

## Trigger-only wake checks

1. Enable wake listening with **Hey Miller**.
2. Say only the phrase. Confirm Miller opens and starts Live without another
   click.
3. Confirm Miller audibly acknowledges that it is ready and listening.
4. Speak one request after the acknowledgement and confirm one response.
5. End Live and confirm wake listening rearms exactly once.
6. Save a custom phrase without restarting Miller and repeat steps 2–5.
7. Start Live manually and confirm it does not produce the wake-only
   acknowledgement.

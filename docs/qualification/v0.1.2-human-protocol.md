# Miller v0.1.2 owner-visible M1 protocol

Status: `RELEASE_APPROVED_WITH_ACCEPTED_DEFERRALS`

Release marker: `MILLER_V0_1_2_RELEASE_APPROVED`

Headless terminal result: `HEADLESS_RELEASE_READY_HUMAN_GATE_NOT_RUN`

Qualified owner-visible app source: `71716f2`

This record combines the immutable headless result with sanitized owner-visible
evidence gathered on 2026-08-15 HST. It retains no transcript text, audio,
credentials, provider payloads, or private runtime logs.

## Required M1 checks

| Check | Result |
| --- | --- |
| External Codex readiness/timeout plus one typed turn | `PASS` |
| Overlay/full-window selection and Command-C | `PASS` |
| GPT-Live speech/transcript/interrupt/end/second session/cleanup | `PASS` |
| Default/custom wake phrase | `PASS` |
| Typed fallback with wake disabled and Live unavailable | `DEFERRED_BY_OWNER` |
| Reset/removal/relaunch with no lingering helper or microphone owner | `DEFERRED_BY_OWNER` |

## Wake recovery evidence

- A quiet-room observation produced no spontaneous activation.
- The default phrase opened Miller and started Live without another click.
- A completed wake-started Live session rearmed wake capture.
- A second phrase activated a second Live session without resaving the phrase.
- The first rearm encountered a Core Audio configuration change. Miller
  recreated capture and accepted the second phrase afterward.
- A custom phrase was saved and exercised without restarting Miller.
- Moving focus away from Miller and closing Settings did not disable wake
  listening.
- Initial and follow-up latency were acceptable in the final owner run. The
  earlier slow first start remains a nonblocking optimization candidate.

## Other owner-visible evidence

- External Codex readiness, a typed turn, and truthful timeout behavior passed.
- Transcript selection, Command-C, Command-V, Markdown, and external links
  passed in the qualified app.
- Live speech, transcripts, mute, interruption, session end, and a fresh
  second session passed.
- Normal session termination released and reacquired microphone ownership as
  required by the wake-to-Live handoff.

## Remaining rows

The owner accepted deterministic coverage for the combined typed-fallback row.
Creating that exact human state requires intentionally making Live unavailable
while preserving a safe typed provider path.

The owner deferred destructive Reset Miller and full removal testing to a
future signed, clean-account build. Normal process stop and relaunch were
observed. Neither deferral weakens the qualified runtime flows above.

## Boundary facts

- Codex remains a separately installed external prerequisite. Miller does not
  bundle, download, update, or remove it.
- Wake listening defaults off and uses the system-default microphone.
- The wake phrase is activation only. It starts one ordinary Live session and
  adds no synthetic durable user turn.
- Packet 3 ended `INCONCLUSIVE`. It did not authorize typed input during an
  active Live session, `initialItems`, persistence changes, or attachments.
- Signing, notarization, and binary publication remain deferred. The owner
  authorized the v0.1.2 source release and tag on 2026-08-15 HST.

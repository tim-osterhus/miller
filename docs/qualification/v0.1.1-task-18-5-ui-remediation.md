# Miller v0.1.1 Task 18.5 UI remediation

Date: 2026-08-08

This is a sanitized headless implementation report for the bounded Task 18.5
presentation remediation. It does not claim Task 19 completion or release
approval.

## RED evidence before production changes

- ./scripts/test.sh --filter CapabilityActivityPresentationTests failed against
  the pre-remediation activity view: 3 focused tests recorded 17 issues for the
  missing presentation state, disclosure/task contract, empty-panel behavior,
  and bounded scrolling contract.
- ./scripts/test.sh --filter SettingsNavigationTests failed against the
  pre-remediation settings shell: the revised fixed-sidebar contract recorded
  10 issues while the existing persistence, window minimum, settings-content,
  and credential-scope tests passed.

## Commands and focused results

- ./scripts/bootstrap-gateway-dependencies.sh — completed with
  MILLER_GATEWAY_DEPENDENCIES_READY=1.
- git diff --check — passed.
- ./scripts/test.sh --filter CapabilityActivityPresentationTests — 6 Swift
  tests passed; the script's JavaScript preflight passed 49 tests.
- ./scripts/test.sh --filter FollowTailStateTests — 13 Swift tests passed;
  the script's JavaScript preflight passed 49 tests.
- ./scripts/test.sh --filter FollowTailPresentationTests — 7 Swift tests
  passed; the script's JavaScript preflight passed 49 tests.
- ./scripts/test.sh --filter TranscriptSelectionPresentationTests — 5 Swift
  tests passed; the script's JavaScript preflight passed 49 tests.
- ./scripts/test.sh --filter SettingsNavigationTests — 8 Swift tests passed;
  the script's JavaScript preflight passed 49 tests.
- ./scripts/test.sh --filter PresentationTests — 125 Swift tests passed; the
  script's JavaScript preflight passed 49 tests.

## Independent review

- Luna Max specification review — APPROVED after one bounded repair pass; no
  P0, P1, or P2 findings remained. Its external-scratch focused suites passed
  6 capability, 7 follow-tail, and 8 settings tests.
- Luna Max adversarial quality review — APPROVED; no P0, P1, or P2 findings.
  Its isolated focused run passed 26 presentation, navigation, and selection
  tests, and `git diff --check` passed.

## Full verification, package, and cleanup

- ./scripts/test.sh — JavaScript preflight passed 49 tests and the complete
  Swift run passed 864 tests.
- (cd Gateway && /opt/homebrew/opt/node@22/bin/npm test) — 115 tests passed;
  one explicit opt-in Task 18 route test was skipped as designed.
- The forbidden-scope diff across package manifests, capabilities, storage,
  Gateway, Live Voice, and Gateway JavaScript was empty.
- ./scripts/package-release-app.sh produced the unsigned release candidate in
  183,452 ms. Finder residue outside the signed bundle was removed before the
  final verification; no package byte was changed.
- ./scripts/verify-release-package.sh .artifacts/release/Miller.app — verified
  2,129 inventoried files and emitted MILLER_RELEASE_PACKAGE_VERIFIED=1.
- ./scripts/clean.sh --preserve-release — passed after the transient package
  measurement was recorded and removed. `.build`, `.cache`,
  `Gateway/node_modules`, package staging, test artifacts, and optional wake
  inputs are absent; the verified release app and inventory remain.

## Remaining gates

- Human/owner visual verification of the activity panel and settings sidebar:
  not run.
- Task 19, Task 20, and release approval: not claimed or run.

MILLER_V0_1_1_TASK_18_5_READY_HUMAN_NOT_RUN

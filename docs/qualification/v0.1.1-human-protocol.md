# Miller v0.1.1 human protocol

Status: `NOT_RUN`
Deterministic marker: `MILLER_V0_1_1_READY_HUMAN_NOT_RUN`

This is a checklist for a later owner-visible pass. Every row is intentionally
`NOT_RUN` in this Task 18 qualification. Do not infer a human result from
synthetic fixtures or local tests.

## Live Voice

| Row | Status |
| --- | --- |
| Start Live Voice requests microphone access only after the explicit action | NOT_RUN |
| Microphone input reaches the selected external Codex App Server | NOT_RUN |
| Remote WebRTC audio is audible once and stops on end or interrupt | NOT_RUN |
| Mute/unmute changes capture without creating a second media session | NOT_RUN |
| Denied permission and missing external runtime leave typed operation usable | NOT_RUN |

## Text and history

| Row | Status |
| --- | --- |
| Live text is selectable without selecting audio | NOT_RUN |
| Saved text remains text-only and does not imply saved audio | NOT_RUN |
| History is reviewed explicitly from the selected conversation | NOT_RUN |
| Export, deletion, reset, and app removal present bounded outcomes | NOT_RUN |

## MCP and provider portability

| Row | Status |
| --- | --- |
| MCP setup shows identity, endpoint class, and declared tools before trust | NOT_RUN |
| Read-only, ask-before-changes, and fully trusted policies are understandable | NOT_RUN |
| Approval and denial outcomes are visible as bounded audit events | NOT_RUN |
| Codex-only account app behavior is clear | NOT_RUN |
| Typed provider portability remains clear when Live Voice is unavailable | NOT_RUN |

## Cleanup and safety

| Row | Status |
| --- | --- |
| Storage/cache cleanup leaves only the intended release inspection artifact | NOT_RUN |
| No owner account, real provider, browser, microphone, audio device, or clipboard is used | NOT_RUN |
| v0.1.2 wake foundation remains absent from the application | NOT_RUN |
